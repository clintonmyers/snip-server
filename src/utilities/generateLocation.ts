// Pumps out default location with no special conditions for now.
// In the future, this will pull a random location from the list
// of all locations.

const generateLocation = (): GameLocation => {
  return { id: 1, name: 'test' };
};
